from django.conf.urls.defaults import *

# feeds = {
# 
# }
# 
# urlpatterns = patterns('django.contrib.syndication.views',
#     url(r'^legislators/(?P<govtrack_id>\d+)/votes/$', 'feed', {'feed_dict': feeds}),
# )
